const help = require ('./helpers');



describe('striker', ()=> {
  it('increments strikes', ()=> {

    const expected = 1;
    const actual = help.striker(0)
    const strikeout = help.striker(3)
  })

})